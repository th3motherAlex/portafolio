<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Proyecto 3</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Kode+Mono:wght@400..700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <style>
        h1 {
            font-family: 'Kode Mono', monospace;
            color: red;
        }
    </style>
</head>
<body>

    <h1>Proyecto 3</h1>

    <div class="container">
        <div class="row">
            <div class="col-12 text-center">
                <button id="boton" class="btn btn-primary m-3">Mostrar/Ocultar imagen</button>
                <img id="imagen" class="w-100" src="imagenes/lasalle.png" alt="">
            </div>
        </div>
    </div>
    <script>

        let boton = document.getElementById("boton");
        boton.addEventListener("click", function() {
            let imagen = document.getElementById("imagen");
            if(imagen.style.display == "none") {
                imagen.style.display = "block";
            }
            else {
                imagen.style.display = "none";
            }
        });

        let numero = 100;
        let texto = "Hola mundo";
        let texto2 = 'Hola mundo 2';
        let booleano = true;
        let arreglo = [1, 2, 3, 4, 5];
        let arregloCadenas = ["Hola", "Mundo cruel", "Adios La Salle"];
        console.log(arregloCadenas);
        let arregloMixto = [1, "Hola", true, 3.14];
        console.log(arregloMixto[3]);

        arregloMixto[1] = "Mundo";

        console.log(arregloMixto);

        let objeto = {
            nombre : "Alex",
            edad: 25,
            soltero: true
        };

        let estudiante = {
            nombre : "carlos",
            matricula : "78886",
            carrera : "Ingenieria en Sistemas",
            materias : ["Programacion", "Base de Datos", "Redes"],
            amigos : [{
                nombre : "Cheche",
                edad : 21
            },
            {
                nombre : "Carlos",
                edad : 32
            }
            ]
        };
        console.log(estudiante.matricula);
        console.log(estudiante.materias[2]); 
        console.log(estudiante.amigos.edad);
        console.log(estudiante.amigos[1].edad);

        let a = 10;
        let b = 20;

        let sumar = a + b;
        let restar = a - b;
        let multiplicar = a * b;
        let dividir = a / b;

        let residuo = 48 % 7;
        console.log(residuo);

        let mayor = a > b;
        let menor = a < b;
        let mayorIgual = a >= b;
        let menorIgual = a <= b;
        let igual = a == b;

        a = -5;
        b = 10*(-1);

        if( a != b) {
            console.log("Son diferentes");
        }
        else {
            console.log("Son iguales");
        }

        //Operador ternario
        let comparacion = a > b ? "Si, a es mayor a b " : "NO, es mayor";
        console.log(comparacion);





        
    








        



    </script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script> 
</body>
</html>
